@model IList<Core_R47.Models.dept2>
@using Core_R47.Models
@{
    ViewBag.Title = "ShowMe";
    ViewBag.sid = Context.Request.Query["sid"];
   // Layout = null;
}

<h2>ShowMe</h2>
<div class="container">
    <div class="col-sm-4">
        <ol>
            @foreach (var a in Model)
            {


            <li>


                @*<a asp-action="showme" asp-route-id="@a.deptid">@a.deptname-(Products:)</a>*@
                @*@a.items2.Count()*@
                @Html.ActionLink(@a.deptname + "- (Products: "  + ")", "showme", new { sid = @a.deptid })
                <input id="operation" type="button" class="btn btn-info btn-xs" onclick="changeurl('@a.deptid')" value="Edit" />
            </li>
                @*@Html.ActionLink("Edit", "showme", new { sid = @a.deptid }, new { onclick = "changeurl('@a.deptid')" })*@
            }
        </ol>
        <button id="operation" type="button" class="btn btn-info btn-lg" data-toggle="modal" data-target="#myModal">New</button>
    </div>
    <div class="col-sm-8">
        @*@Html.Action("showitems", new { sid = ViewBag.sid })*@
        @*<a asp-action="showitems" asp-route-id="@ViewBag.sid">showitems</a>*@
        @*@{Html.RenderPartial("showitems", new Dept_Items { sid = ViewBag.sid }); }*@
    </div>
</div>

<style>
    .modal-dialog {
        width: 80%;
        margin: 30 auto;
    }
    
.modal-dialog {
    max-width: 5000px;
    margin: 1.75rem auto;
}
              
</style>


<div id="myModal" class="modal fade" role="dialog">
    <div class="modal-dialog modal-lg" style="width:100%">

        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Operation in CRUD</h4>
                <button type="button" class="close" data-dismiss="modal">&times;</button>

            </div>
            <div class="modal-body">

                @*@Html.RenderAction("create2", new { sid = ViewBag.sid })*@
                @*<a asp-action="create2" asp-route-id="@ViewBag.sid">create2</a>*@
                @{Html.RenderPartial("create2", new Dept_Items { sid = ViewBag.sid }); }


            </div>


            <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-dismiss="modal">Close</button>
            </div>
        </div>

    </div>
</div>


<script type="text/javascript">
    // $("#myModal").modal();

    function changeurl(a) {
        // localStorage.name = a;
        // localStorage.setItem('test', a);
        // window.location.href = "?sid=" + a;
        // alert(a);
        //jQuery.noConflict();
        //$.cookie('name', a);
        // gvar = a;
        //var url1 = window.location;
        //var url = new URL(url1);
        //url.searchParams.set("sid", a);
        abc()
    }
    function abc() {
        window.setTimeout(function () {
            $('#myModal').modal();
        }, 300);
    }
    $(document).ready(function () {
        if (gvar == 100) {
            $('#myModal').modal();
        }
        gvar = 0;
        //let searchParams = new URLSearchParams(window.location.search)
        //// alert(searchParams.has('sid'));
        //if (searchParams.has('sid')) {
        //    $('#myModal').modal();
        //}
    })
</script>



